<!-- Header -->
<header class="bg-zinc-950 text-white">
  <div class="max-w-7xl mx-auto px-4 py-4 flex items-center">
    <img src="<%= typeof getLink !== 'function' ? '/assets/logo.png' : getLink('/assets/logo.png') %>" alt="VisionBoard Logo" class="h-8 mr-3">
    <a href="<%= typeof getLink !== 'function' ? '/' : getLink('/') %>" class="text-amber-400 text-xl font-semibold">VisionBoard Reports</a>
  </div>
</header>
  